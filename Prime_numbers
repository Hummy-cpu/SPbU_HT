let Prime n =
    if n < 2 then false
    else
        let maxDiv = int (sqrt (float n))
        [2..maxDiv] |> List.forall (fun x -> n % x <> 0)


let primes =
    Seq.initInfinite (fun i -> i + 2) 
    |> Seq.filter Prime
